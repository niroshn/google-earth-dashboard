# Example environment variables for React-GEE-Mapper
# Copy this file to .env and update with your actual values

# =============================================================================
# GOOGLE EARTH ENGINE CONFIGURATION
# =============================================================================

# Google Earth Engine Project ID (from Google Cloud Console)
# Required for all deployments
GOOGLE_EARTH_ENGINE_PROJECT_ID=your-project-id-here

# =============================================================================
# AUTHENTICATION OPTIONS (choose one)
# =============================================================================

# Option 1: User Authentication (for local development)
# Run: earthengine authenticate
# Then: earthengine set_project YOUR_PROJECT_ID
# No additional environment variables needed

# Option 2: Service Account Authentication (recommended for production)
# Create a service account in Google Cloud Console with Earth Engine permissions
# Download the JSON key file

# Service account email address
# GOOGLE_SERVICE_ACCOUNT_EMAIL=your-service-account@your-project.iam.gserviceaccount.com

# Service account key - can be provided in multiple ways:
# Method A: Relative path to JSON key file in backend directory (RECOMMENDED)
# 1. Download your service account JSON key file from Google Cloud Console
# 2. Place the JSON file in the backend/ directory (same folder as this .env file)
# 3. Reference it by filename only (the file is automatically ignored by git)
# GOOGLE_SERVICE_ACCOUNT_KEY=your-service-account-key.json
#
# Method B: Absolute file path to JSON key file
# GOOGLE_SERVICE_ACCOUNT_KEY=/path/to/your-service-account-key.json
#
# Method C: Base64 encoded JSON key (useful for CI/CD)
# GOOGLE_SERVICE_ACCOUNT_KEY=ewogICJ0eXBlIjogInNlcnZpY2VfYWNjb3VudCIsCiAgInByb2plY3RfaWQiOi4uLg==
#
# Method D: Raw JSON string (escape quotes properly)
# GOOGLE_SERVICE_ACCOUNT_KEY='{"type": "service_account", "project_id": "your-project", ...}'
#
# SECURITY NOTE: JSON key files are automatically excluded from git via .gitignore
# Never commit service account keys to version control!

# =============================================================================
# AWS CONFIGURATION (for Lambda deployment)
# =============================================================================

# AWS Credentials (get from AWS IAM)
# Create an IAM user with programmatic access and Lambda deployment permissions
AWS_ACCESS_KEY_ID=your_aws_access_key_here
AWS_SECRET_ACCESS_KEY=your_aws_secret_key_here
AWS_REGION=us-east-1

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================

# For local development:
# 1. Set GOOGLE_EARTH_ENGINE_PROJECT_ID
# 2. Run: earthengine authenticate
# 3. Run: earthengine set_project YOUR_PROJECT_ID

# For production deployment:
# 1. Create a service account in Google Cloud Console
# 2. Enable Earth Engine API for the project
# 3. Grant the service account "Earth Engine Resource Admin" role
# 4. Download JSON key file
# 5. Set GOOGLE_SERVICE_ACCOUNT_EMAIL and GOOGLE_SERVICE_ACCOUNT_KEY variables
# 6. Set AWS credentials: AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, AWS_REGION
